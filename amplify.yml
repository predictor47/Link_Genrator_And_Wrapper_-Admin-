version: 1
backend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm install -g aws-cdk
        - cdk bootstrap aws://868948464569/us-east-1
    build:
      commands:
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm i -g @aws-amplify/cli
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
